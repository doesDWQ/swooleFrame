1，能够包含http和websocket内置服务器，并且能够正常启动，（万里长征第一步搞定）
2，能够实现自动加载（已经实现,补充了递归加载的那一步），此处产生一条必须准守的规定，命名空间必须是以app同级的目录开始命名
2.1，封装好各站swoole的操作类（task的封装已经搞定，可能还有一些细节不是很完备)
3，实现，表和模型的自动创建(类似于一个web系统一样的东东)
4，封装上各种redis,mysql,同异步操作类
5,路由分发还没搞定啊(等，没什么技术难度,分发请求已经完成，只能pathinfo形式的)
6,需要加上切面编程思想(已经搞定)
7,需要编写一套日志系统来记录抛出的异常！因为一抛出异常swoole就被中断了(已搞定)
8，封装cookie和session类(session需要手动封装,cookie已经自带了，session在swoole里面没有被实现需要自己实现)
9，需要编写进程池代码，所有所有对象的一个注册之地(已经搞定）
10，sql文件的自动创建  (搞定)
11，需要加上状态码啥的。

2018-10-4日，初步完成框架雏形




注意事项：因为swoole程序是常驻内存的，所以很多变量什么的使用完成后记得释放掉。