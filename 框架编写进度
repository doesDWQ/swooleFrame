1，能够包含http和websocket内置服务器，并且能够正常启动，（万里长征第一步搞定）
2，能够实现自动加载（已经实现），此处产生一条必须准守的规定，命名空间必须是以app同级的目录开始命名
2.1，封装好各站swoole的操作类（task的封装已经搞定)
3，实现，表和模型的自动创建(类似于一个web系统一样的东东)
4，封装上各种redis,mysql,同异步操作类
5,路由分发还没搞定啊(等，没什么技术难度)
6,需要加上切面编程思想
7,需要编写一套日志系统来记录抛出的异常！因为一抛出异常swoole就被中断了
8，封装cookie和session类(session需要手动封装,cookie已经自带了)
9，需要编写进程池代码，所有所有对象的一个注册之地





注意事项：因为swoole程序是常驻内存的，所有很多变量什么的使用完成后记得释放掉。